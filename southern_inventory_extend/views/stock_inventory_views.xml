<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Physical Inventory Tree View - Main inventory adjustment screen -->
    <record id="view_stock_quant_inventory_tree_inherit" model="ir.ui.view">
        <field name="name">stock.quant.inventory.tree.editable.inherit</field>
        <field name="model">stock.quant</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_inventory_editable"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='inventory_diff_quantity']" position="after">
                <field name="company_currency_id" column_invisible="1"/>
                <field name="valuation_adjustment"
                       widget="monetary"
                       options="{'currency_field': 'company_currency_id'}"
                       decoration-danger="valuation_adjustment &lt; 0"
                       decoration-success="valuation_adjustment &gt; 0"/>
            </xpath>
        </field>
    </record>
</odoo>